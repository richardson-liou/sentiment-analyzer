# Sentiment Analyzer

A web app that analyzes the sentiment of text using a pretrained HuggingFace model.

# Run

git clone https://github.com/richardson-liou/sentiment-analyzer.git
cd sentiment-analyzer
pip install -r requirements.txt
streamlit run app.py